#  How will you set the starting value in generating random numbers?

import random

# Set the seed value (replace 42 with any integer)
random.seed(42)

# Generate random numbers
random_number = random.random()
